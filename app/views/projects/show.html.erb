<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @project.name %>
</p>

<p>
  <strong>User:</strong>
  <%= @project.user.name %>
</p>

<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'New Server', new_server_path %> 

</br>
</br>
</br>

<div>
    <% if @project.servers.any? %>
      <ol>
        <%= render @servers %>
      </ol>
    <% end %>
</div>
